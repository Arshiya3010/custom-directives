import { ChangemeDirective } from './changeme.directive';

describe('ChangemeDirective', () => {
  it('should create an instance', () => {
    const directive = new ChangemeDirective();
    expect(directive).toBeTruthy();
  });
});
